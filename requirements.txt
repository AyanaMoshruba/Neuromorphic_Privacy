torch  # Replace with your version
torchvision  # For image datasets like MNIST, FMNIST, CIFAR10, CIFAR100
snntorch  # For spiking neural network support
numpy  # General numerical operations
scikit-learn  # For dataset preprocessing, evaluation, SVM
matplotlib  # For plotting ROC curves
tqdm  # For progress bars
opacus  # For privacy-preserving learning
argparse  # CLI argument parsing (already part of Python stdlib)
pandas  # Optional: if handling tabular data (common in datasets like breast cancer)
seaborn  # Optional: for advanced data visualization
